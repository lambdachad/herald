[package]
name = "herald"
version = "0.1.0"
edition = "2024"

[dependencies]
ctrlc = "3"
cpal = "0.17"
parakeet-rs = { version = "0.3", default-features = false, features = ["ort-defaults", "cuda"] }
